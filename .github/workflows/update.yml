name: Remote Update

on:
  repository_dispatch:
    types: [discord-update]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          ${{ secrets.GIT_COM }}